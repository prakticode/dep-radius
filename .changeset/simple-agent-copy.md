---
"@dep-radius/core": patch
"dep-radius": patch
---

The package descriptions, keywords and READMEs now describe radius as it is used: it tells an AI
agent which changes in a dependency upgrade affect the code, with the files and lines to check.
